[project]
name = "glance"
version = "0.1.0"
description = "Simple web-based file explorer for browsing repos"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

[project.scripts]
explorer = "main:app"
